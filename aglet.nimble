# Package

version       = "0.1.0"
author        = "liquid600pgm"
description   = "A safe, high-level, optimized OpenGL wrapper"
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.0.6"
requires "nimterop >= 0.4.4"
requires "glm >= 1.1.1"
